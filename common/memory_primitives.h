#ifndef MEMORY_PRIMITIVES_H
#define MEMORY_PRIMITIVES_H

#include <cstddef>

// just an id in a global set of registers
using Register = size_t;

// just a memory cell in a shared memory
using MemoryCell = size_t;

#endif //MEMORY_PRIMITIVES_H
